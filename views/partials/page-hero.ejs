<section class="page-hero">
  <div class="container page-hero-inner">
    <p class="eyebrow"><%= eyebrow || 'Delamere Farm' %></p>
    <h1><%= heading || title %></h1>
    <% if (lead) { %><p><%= lead %></p><% } %>
  </div>
</section>
